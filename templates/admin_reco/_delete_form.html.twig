<form method="post" action="{{ path('app_admin_reco_delete', {'id': recomendation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recomendation.id) }}">
    <button class="btn btn-danger border-danger"><i class="bi bi-trash3-fill ms-2"></i>Supprimer</button>
</form>
